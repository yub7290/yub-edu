<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yub.edu.biz.mapper.EduExamChapterQuestionConfigMapper">

    <resultMap id="EduExamChapterQuestionConfigMap" type="com.yub.edu.biz.entity.EduExamChapterQuestionConfig">
        <id column="id" property="id"/>
        <result column="exam_id" property="examId"/>
        <result column="chapter_id" property="chapterId"/>
        <result column="question_type" property="questionType"/>
        <result column="question_count" property="questionCount"/>
        <result column="score_per_question" property="scorePerQuestion"/>
    </resultMap>

    <sql id="baseColumns">
        id, exam_id, chapter_id, question_type, question_count, score_per_question
    </sql>

    <insert id="insertBatch">
        INSERT INTO edu_exam_chapter_question_config (exam_id, chapter_id, question_type, question_count, score_per_question)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.examId}, #{item.chapterId}, #{item.questionType}, #{item.questionCount}, #{item.scorePerQuestion})
        </foreach>
    </insert>

    <delete id="deleteByExamId">
        DELETE FROM edu_exam_chapter_question_config WHERE exam_id = #{examId}
    </delete>

    <select id="selectByExamId" resultMap="EduExamChapterQuestionConfigMap">
        SELECT <include refid="baseColumns"/>
        FROM edu_exam_chapter_question_config
        WHERE exam_id = #{examId}
        ORDER BY chapter_id ASC, question_type ASC
    </select>
</mapper>
